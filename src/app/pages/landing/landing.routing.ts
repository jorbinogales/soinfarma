import { Route } from '@angular/router';
import { LandingComponent } from './landing.component';

export const LandingRouting: Route[] = [
    {
        path:'',
        component: LandingComponent,
    },
];
